<h2 id="title">Candidate Selection</h2><br>
<div class="container-fluid" id="box">
  <div class="table-responsive">
    <table class="table table-hover" id="nomineeTable">
      <thead>
        <tr>
          <th>#</th>
          <th>Approved</th>
          <th>Nominee Name</th>
          <th>CV</th>
          <th>Statement</th>
        </tr>
      </thead>
      <% @count = 0 %>
      <% @nominees.each do |nominee| %>
      <tr>
      <%= simple_form_for(nominee, :html => {:multipart => true, :class => 'form-inline'}) do |f| %>
      <% @count = @count + 1 %>
        <td><%= @count %></td>
        <td><%= f.input :committee_check, as: :boolean, boolean_style: :inline, label: false %></td>
        <td><%= nominee.name %></td>
        <td><%= nominee.statement %></td>
        <td><%= nominee.cv %></td>
      <% end %>
      </tr>
      <% end %>
    </table>
  </div>
</div>
